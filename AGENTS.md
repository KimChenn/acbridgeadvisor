# AGENTS.md

## Project context

This is a standard Vite + React frontend app. Keep changes focused on the requested feature and preserve the existing project conventions.

Start with `README.md` for local setup and environment variables.

## Key files

- `src/`: frontend application source.
- `vite.config.js`: Vite config.
- `.env.local`: local-only environment values; never commit secrets.

## Working notes

- Use `npm run dev` for local frontend development.
- Prefer the existing app structure before introducing new framework patterns.
- Run the relevant checks from `package.json` before finishing code changes.
